@Namespace list
#Args: list_name, str type >>"str"/"num"
#Returns -> randomized (Random value from first List)

function list_random(str list_name, str type);{
    suppress_errors()
    num __list_len >>$list_name$.length
    ;;num f >>rand:0,__list_len;;get$type$ListValue($list_name$[f])
    $type$ randomized >>$list_name$[f]
    return randomized;
}list_random;



